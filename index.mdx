---
sidebarTitle: "Introduction"
mode: "wide"
keywords: ["overview", "platform", "getting started", "features"]
---

export const HeroCard = ({ filename, title, description, href }) => {
  return (
    <a className="group cursor-pointer pb-8" href={href}>
      <img
        src={`https://mintlify.s3.us-west-1.amazonaws.com/mintlify/images/hero/${filename}.png`}
        className="block dark:hidden pointer-events-none group-hover:scale-105 transition-all duration-100"
      />
      <img
        src={`https://mintlify.s3.us-west-1.amazonaws.com/mintlify/images/hero/${filename}-dark.png`}
        className="pointer-events-none group-hover:scale-105 transition-all duration-100 hidden dark:block"
      />
      <h3 className="mt-5 text-gray-900 dark:text-zinc-50 font-medium">
        {title}
      </h3>
      <span className="mt-1.5">{description}</span>
    </a>
  );
};

<div className="relative">
  
  <div className="relative z-10 px-4  lg:pb-24 max-w-3xl mx-auto">
    <h1 className="block text-4xl font-medium text-center text-gray-900 dark:text-zinc-50 tracking-tight">
      Welcome to Unsiloed AI
    </h1>

    <div className="max-w-xl mx-auto px-4 mt-4 text-lg text-center text-gray-500 dark:text-zinc-500">
      The unstructured data interface for LLMs and AI agents. Turn complex documents into structured Markdown and JSON.
    </div>

    <div className="px-6 lg:px-0 mt-6 lg:mt-12 grid sm:grid-cols-2 gap-x-6 gap-y-4">
      <HeroCard filename="rocket" title="Quickstart" description="Explore our complete API documentation with interactive examples" href="/quickstart" />

      <HeroCard
        filename="cli"
        title="SDK & API"
        description="Official SDKs for Python and JavaScript, or use the REST API directly"
        href="/document-processing/parsing"
      />
    </div>
  </div>
</div>

<div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 py-12">

## What is Unsiloed AI?

Unsiloed AI is the unstructured data interface for LLMs and AI agents. We build **vision-first, layout-aware systems** that combine computer vision, OCR, and multimodal models to turn complex documents into **deterministic, machine-readable representations** so that AI agents can read from and write to documents as reliably as humans.

Modern document workflows break down on real-world data: PDFs with dense layouts, scanned forms, tables, charts, images, and inconsistent formatting. Generic OCR and text-only LLM parsers lose structure, hallucinate, or produce brittle outputs that fail in production. Unsiloed AI is designed to be the infrastructure layer that sits before your LLMs, agents, or RAG pipelines ensuring documents are parsed correctly and consistently.

You might find Unsiloed AI useful if you want to:

- **Parse unstructured documents** (PDFs, PPTs, images, Excel sheets, and 20+ file formats) into high-fidelity **Markdown and JSON**
- **Extract structured data** from documents using **schema-driven key-value extraction** along with **citation and confidence scoring**
- **Classify or split large files** into logical buckets, sections, or categories

These capabilities are commonly used for **document-based workflow automation**, **reliable parsing and chunking for RAG**, and **agentic systems that need structured access to documents**.

Think of Unsiloed AI as the interface layer between your unstructured data and any **LLM, AI agent, or automation** you are building on top of it.

## Core Capabilities

<CardGroup cols={2}>
  <Card
    title="Layout-Aware Parsing"
    icon="file-text"
  >
    Parse complex documents into structured, hierarchical chunks while preserving layout, reading order, and visual hierarchy. Natively understands tables, images, charts, plots, formulas, and headers, producing Markdown and JSON optimized for accurate chunking and RAG.
  </Card>
  <Card
    title="Schema-Based Extraction"
    icon="database"
  >
    Extract structured data using custom JSON schemas with deterministic outputs. Every field includes word-level citations, bounding boxes, and confidence scores for full traceability back to the source document.
  </Card>
  <Card
    title="Smart Splitting"
    icon="scissors"
  >
    Split large or merged files into logical documents or sections using layout, content, or rule-based logic. Ideal for processing scanned batches, multi-document PDFs, or downstream routing.
  </Card>
  <Card
    title="Document Classification"
    icon="tags"
  >
    Classify documents by type using visual and semantic signals. Route each document to the appropriate parsing, extraction, or automation pipeline.
  </Card>
</CardGroup>

## Why Unsiloed AI?

<CardGroup cols={2}>
  <Card
    title="Built for Production"
    icon="bolt"
  >
    - Asynchronous processing for large and multi-page documents
    - Deterministic outputs with confidence scores and word level bounding boxes
    - Broad multi-format support across PDFs, DOCX, PPTX, images and more
    - Scalable infrastructure for high-throughput enterprise workloads
  </Card>
  <Card
    title="Developer-First"
    icon="laptop-code"
  >
    - Clean REST APIs designed with stable versioned contracts
    - Schema-driven extraction with validation, confidence and traceability
    - Interactive playground for testing API requests schemas and outputs
    - Predictable error handling for reliable production integrations
  </Card>
</CardGroup>

## Common Use Cases

<CardGroup cols={2}>
  <Card
    title="Finance"
    icon="chart-line"
  >
    Parse financial statements, reports, and regulatory filings into structured, machine-readable data.
  </Card>
  <Card
    title="Legal"
    icon="scale-balanced"
  >
    Extract clauses, entities, dates, and obligations from contracts and legal documents.
  </Card>
  <Card
    title="Healthcare"
    icon="heart-pulse"
  >
    Structure clinical documents, forms, and records for downstream systems and workflows.
  </Card>
  <Card
    title="RAG & Automation"
    icon="robot"
  >
    Parse, chunk, classify, and route documents to power reliable RAG pipelines and document-driven automations.
  </Card>
</CardGroup>

## SDKs

We provide official SDKs to make integration even easier:

<CodeGroup>

```bash Python
pip install unsiloed-sdk
```

```bash JavaScript/TypeScript
npm install unsiloed-sdk
```

</CodeGroup>

<CardGroup cols={2}>
  <Card
    title="Python SDK"
    icon="python"
    href="https://pypi.org/project/unsiloed-sdk/"
  >
    Official Python SDK with sync and async support
  </Card>
  <Card
    title="JavaScript SDK"
    icon="js"
    href="https://www.npmjs.com/package/unsiloed-sdk"
  >
    Official JavaScript/TypeScript SDK with full type definitions
  </Card>
</CardGroup>

## Quick Start Examples

<CodeGroup>

```python Python
from unsiloed_sdk import UnsiloedClient

with UnsiloedClient(api_key="your-api-key") as client:
    result = client.parse_and_wait(file="document.pdf")
    print(f"Total chunks: {result.total_chunks}")
```

```javascript JavaScript
const { UnsiloedClient } = require('unsiloed-sdk');

const client = new UnsiloedClient({ apiKey: 'your-api-key' });

const result = await client.parseAndWait({ file: 'document.pdf' });
console.log(`Total chunks: ${result.total_chunks}`);
```

```typescript TypeScript
import { UnsiloedClient } from 'unsiloed-sdk';

const client = new UnsiloedClient({ apiKey: 'your-api-key' });

const result = await client.parseAndWait({ file: 'document.pdf' });
console.log(`Total chunks: ${result.total_chunks}`);
```

```bash cURL
# Parse a document
curl -X POST "https://prod.visionapi.unsiloed.ai/parse" \
  -H "api-key: your-api-key" \
  -F "document_file=@document.pdf"

# Get results
curl -X GET "https://prod.visionapi.unsiloed.ai/parse/{job_id}" \
  -H "api-key: your-api-key"
```

</CodeGroup>

## Getting Started

<Steps>
  <Step title="Get API Access">
    [Sign up on Unsiloed AI](https://cal.com/aman-mishra-p0ry57/15min) to get your API key and get started.
  </Step>
  <Step title="Install SDK or Use API Directly">
    Install our Python or JavaScript SDK, or use the REST API directly. Check out the [installation guide](/document-processing/vision-api) to get started.
  </Step>
  <Step title="Make Your First Request">
    Use our SDK or API to extract data from a document. Check out the [extraction guide](/document-processing/vision-api) to get started.
  </Step>
  <Step title="Explore Advanced Features">
    Learn about parsing strategies, classification, splitting, and batch processing to build powerful document workflows.
  </Step>
</Steps>

## API Base URL

All API requests should be made to:

```
https://prod.visionapi.unsiloed.ai
```

Authentication is required using your API key in the `api-key` header.

## Need Help?

<CardGroup cols={2}>
  <Card
    title="Documentation"
    icon="book"
    href="/document-processing/parsing"
  >
    Explore our comprehensive guides and API reference
  </Card>
  <Card
    title="Support"
    icon="envelope"
    href="mailto:hello@unsiloed-ai.com"
  >
    Contact our team at hello@unsiloed-ai.com
  </Card>
</CardGroup>

</div>